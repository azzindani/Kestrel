setuptools>=83.0.0  # floor for PYSEC-2026-3447 (pip-audit gate in CI)
asyncpg>=0.29.0
ccxt[async]>=4.2.0
python-dotenv>=1.0.0
rich>=13.7.0
numpy>=1.26.0
httpx>=0.27.0
uvloop>=0.19.0

# Optional provider dependencies — install only the one matching your EXCHANGE setting:
# EXCHANGE=oanda  → pip install oandapyV20>=0.6.3
# EXCHANGE=alpaca → pip install alpaca-py>=0.29.0
# EXCHANGE=ibkr   → pip install ib_insync>=0.9.86
# EXCHANGE=exness → pip install metaapi-cloud-sdk   (Exness MT5 via MetaApi cloud)
